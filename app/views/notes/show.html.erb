<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @note.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @note.user_id %>
</p>

<p>
  <strong>Content:</strong>
  <%= @note.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= @note.image %>
</p>

<%= link_to 'Edit', edit_note_path(@note) %> |
<%= link_to 'Back', notes_path %>

<%= form_tag comment_path(@note.id)%>

	内容
	<textarea name="content"></textarea>
	<input tyoe="submit" value="投稿する">
<%end%>
