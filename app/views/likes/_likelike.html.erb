<% if !current_user.likes.find_by(note_id:note.id)%>
  <%= link_to 'いいね', likescreate_path(note.id), remote:true %>
<% else %>
  <%= link_to 'いいね外す', likesdestroy_path(note.id), method: :delete, remote: true %>
<% end %>